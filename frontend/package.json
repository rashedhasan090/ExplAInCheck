{
  "name": "explaincheck-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "python -m http.server 8080",
    "dev": "live-server --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
